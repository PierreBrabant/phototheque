<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head') %>
<body>
    <%-include('partials/nav')%>


        <div class="container">
            <div class="row mt-4">
                <div class="col">
                    <a href="/albums" class="mt-4">&lt;Retout</a>
                    <h1 class="mb-4"><%= album.title %></h1>
                </div>
            </div>
            <div class="row mb-4">
                <div class="col">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">
                                Ajouter une image
                            </h5>
                            <!-- Formulaire pour envoyé une image en récuperant l'id album dans action -->
                            <form method="post" action="/albums/<%=album._id%>" class="form-inline" enctype="multipart/form-data">
                                <input type="file" name="image" class="form-control-file">
                                <button type="submit" class="btn btn-primary">Ajouter</button>
                            </form>
                             <!--Affichage de l'erreur en cas de probleme avec le nom de l'album(album non nommé, nom d'album deja utilisé)-->
                            <% if(error.length){%>
                                <div class="alert alert-danger mt-4 mb-2" role="alert">
                                    <%=error[0]%>
                                </div>
                            <%}%>
                        </div>
                    </div>
                </div>
            </div>
            <div class="row row-cols-1 row-cols-md-4 g-4">
                <%album.images.forEach((image)=>{%>
                <div class="col">
                    <div class="card">
                        <img src="/upload/66f6cd1ea571ebf5c82a7a15/signature.png" alt="" class="card-img-top">
                        <div class="card-body">
                            <h5><%=album.images%></h5>
                            <a href="/albums/delete/<%=album._id%>">Supprimé</a>
                        </div>
                    </div>
                </div>
                <%})%>
            </div>
        </div>


    <%-include('partials/script')%>
</body>
</html>